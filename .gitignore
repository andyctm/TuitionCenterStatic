node_modules/
dist/
build/
coverage/
*.log

# Environment secrets — never commit real values, only .env.example
.env
.env.local
.env.*.local
backend/.env

# Ephemeral one-off scripts/notes (e.g. wizard skill output) — never commit real secrets
.scratch/

# OS/editor cruft
.DS_Store
Thumbs.db
